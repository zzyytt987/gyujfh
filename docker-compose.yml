services:
  backend:
    image: ghcr.io/${GITHUB_REPOSITORY:-user/repo}:latest
    container_name: enterprise-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
